# Domain docs

Treat the repository as one product context. `platform/` and `web/` are implementation areas within it.

Before architecture work, read these when present:

- `CONTEXT.md` at the repository root
- `docs/adr/` for architectural decisions

If they do not exist, proceed without creating them. Use documented domain terms consistently and surface conflicts with existing ADRs.
