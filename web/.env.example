# The same-origin Next.js BFF forwards platform requests here during local
# development. Cloudflare deployments use the PLATFORM service binding.
PLATFORM_API_BASE_URL=http://localhost:8787

# Optional locally. Required in production for the Scale inquiry form.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
